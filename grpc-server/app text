/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import React, { useState } from 'react';
import { ActiveTab, DesignMood } from './types';
import MoodPlayground from './components/MoodPlayground';
import ColorAuditor from './components/ColorAuditor';
import GridSystem from './components/GridSystem';
import { motion, AnimatePresence } from 'motion/react';
import { Layers, Palette, Columns, Monitor, ShieldCheck, Heart, Sparkles } from 'lucide-react';

export default function App() {
  const [activeTab, setActiveTab] = useState<ActiveTab>('playground');
  const [activeMood, setActiveMood] = useState<DesignMood>('swiss');

  // Personal profile constants matching professional credentials style
  const userInitials = "MA";
  const userFullName = "Mohdarif Systems";
  const userRole = "Director of UX & Design";

  return (
    <div className="flex h-screen w-screen bg-slate-50 font-sans overflow-hidden text-slate-900 selection:bg-blue-600/10 selection:text-blue-600 antialiased">
      {/* ── Sidebar Navigation (Professional Polish) ── */}
      <aside className="w-64 bg-slate-900 flex flex-col shrink-0 text-slate-300">
        {/* Workspace Brand Header */}
        <div className="h-16 flex items-center px-6 border-b border-slate-800 shrink-0">
          <div className="w-8 h-8 bg-blue-600 rounded flex items-center justify-center mr-3 shadow-md shadow-blue-500/20">
            <div className="w-4 h-4 border-2 border-white rounded-sm"></div>
          </div>
          <span className="text-white font-semibold tracking-tight uppercase font-display text-sm">Studio Canvas</span>
        </div>

        {/* Vertical Tab Navigation */}
        <nav className="flex-1 py-6 overflow-y-auto space-y-6">
          <div className="px-4">
            <p className="text-[10px] uppercase tracking-widest text-slate-500 font-bold mb-3 px-2">Design Workspace</p>
            <div className="space-y-1">
              {/* Tab 1: Workspace Playground */}
              <button
                id="tab-playground"
                onClick={() => setActiveTab('playground')}
                className={`w-full flex items-center px-3 py-2 rounded-md transition-colors text-left cursor-pointer ${
                  activeTab === 'playground'
                    ? 'bg-slate-800 text-blue-400 font-semibold shadow-sm'
                    : 'text-slate-400 hover:text-white hover:bg-slate-800/40'
                }`}
              >
                <Monitor className="w-4 h-4 mr-3 shrink-0 text-blue-500" />
                <span className="text-sm">Visual Playground</span>
              </button>

              {/* Tab 2: Daltonism & Accessibility */}
              <button
                id="tab-accessibility"
                onClick={() => setActiveTab('accessibility')}
                className={`w-full flex items-center px-3 py-2 rounded-md transition-colors text-left cursor-pointer ${
                  activeTab === 'accessibility'
                    ? 'bg-slate-800 text-blue-400 font-semibold shadow-sm'
                    : 'text-slate-400 hover:text-white hover:bg-slate-800/40'
                }`}
              >
                <Palette className="w-4 h-4 mr-3 shrink-0 text-blue-550" />
                <span className="text-sm">Color Contrast A11y</span>
              </button>

              {/* Tab 3: Grid Baseline scales */}
              <button
                id="tab-grid"
                onClick={() => setActiveTab('grid')}
                className={`w-full flex items-center px-3 py-2 rounded-md transition-colors text-left cursor-pointer ${
                  activeTab === 'grid'
                    ? 'bg-slate-800 text-blue-400 font-semibold shadow-sm'
                    : 'text-slate-400 hover:text-white hover:bg-slate-800/40'
                }`}
              >
                <Columns className="w-4 h-4 mr-3 shrink-0 text-blue-500" />
                <span className="text-sm">Grid & Typo Scales</span>
              </button>
            </div>
          </div>

          <div className="px-4">
            <p className="text-[10px] uppercase tracking-widest text-slate-500 font-bold mb-3 px-2">Design Standard</p>
            <div className="space-y-1">
              <div className="flex items-center px-3 py-2 text-slate-500 text-xs gap-2">
                <span className="w-2 h-2 rounded-full bg-emerald-500 animate-pulse shrink-0" />
                <span className="font-mono text-[10px] uppercase">Rhythm Score: AA+</span>
              </div>
              <div className="flex items-center px-3 py-2 text-slate-500 text-xs gap-2">
                <span className="w-2 h-2 rounded-full bg-blue-500 shrink-0" />
                <span className="font-mono text-[10px] uppercase">Compliance: AAA/AA</span>
              </div>
            </div>
          </div>
        </nav>

        {/* User Profile Card (Match Director profile element) */}
        <div className="p-4 border-t border-slate-800 shrink-0">
          <div className="flex items-center bg-slate-800/50 p-3 rounded-lg">
            <div className="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center text-white text-xs font-bold mr-3 select-none">
              {userInitials}
            </div>
            <div className="overflow-hidden min-w-0">
              <p className="text-xs font-bold text-white leading-none truncate">{userFullName}</p>
              <p className="text-[10px] text-slate-400 mt-1 truncate">{userRole}</p>
            </div>
          </div>
        </div>
      </aside>

      {/* ── Main View Core Workspace ── */}
      <main className="flex-1 flex flex-col overflow-hidden">
        {/* Main Workspace top Header bar */}
        <header className="h-16 bg-white border-b border-slate-200 flex items-center justify-between px-8 shrink-0">
          <div className="flex items-center gap-4">
            <h1 className="text-md font-bold text-slate-800 font-display">
              {activeTab === 'playground' && 'Visual Interface Architect'}
              {activeTab === 'accessibility' && 'WCAG 2.1 Color & Vision Auditor'}
              {activeTab === 'grid' && 'Symmetric Grid & Typographic Ladder'}
            </h1>
            <div className="px-2 py-0.5 bg-green-50 text-green-700 text-[10px] font-bold border border-green-200 rounded uppercase tracking-wider italic shrink-0">
              Live Auditing Suite
            </div>
          </div>

          <div className="flex items-center space-x-6">
            <div className="relative">
              <span className="absolute top-0 right-0 w-1.5 h-1.5 bg-green-500 rounded-full animate-ping"></span>
              <span className="text-xs text-slate-450 text-slate-400 font-mono">● LIVE INTEGRATIONS</span>
            </div>
            <div className="h-8 w-px bg-slate-200"></div>
            <button
              onClick={() => {
                alert("Design parameters evaluated and compiled successfully.");
              }}
              className="bg-blue-600 hover:bg-blue-700 transition text-white px-4 py-2 rounded text-xs font-medium shadow-sm shadow-blue-200 cursor-pointer"
            >
              Export System Variables
            </button>
          </div>
        </header>

        {/* Body Workspace Page Content */}
        <div className="p-8 flex-1 overflow-y-auto space-y-6">
          {/* Performance Introduction Slate Card */}
          <div className="bg-white border border-slate-200 rounded-xl p-6 shadow-sm overflow-hidden flex flex-col md:flex-row items-center justify-between gap-6 relative">
            <div className="flex flex-col gap-1 max-w-2xl">
              <span className="text-[10px] uppercase font-bold text-blue-600 tracking-wider">SYSTEM DIAGNOSTICS & DESIGN AUDIT</span>
              <h2 className="text-xl font-bold font-display text-slate-900 tracking-tight leading-tight mt-1">
                Engineering aesthetic environments through strict mathematical layouts
              </h2>
              <p className="text-xs text-slate-500 leading-relaxed">
                Visual comfort isn't creative luck—it is rigid margin grids, relative sRGB luminance compliance checks, and geometric pixel constraints. Edit the layout properties panel below to verify micro-alignment variables in real time.
              </p>
            </div>

            <div className="bg-slate-50 border border-slate-150 p-4 rounded-xl shrink-0 min-w-[200px] flex flex-col justify-center">
              <span className="text-[9px] uppercase font-bold text-slate-450 text-slate-400 tracking-wider">STYLING ENVIRONMENT</span>
              <div className="flex items-center gap-2 mt-1.5">
                <div className={`w-3 h-3 rounded-full ${
                  activeMood === 'swiss' ? 'bg-slate-900' :
                  activeMood === 'editorial' ? 'bg-amber-600' :
                  activeMood === 'tech' ? 'bg-teal-400' : 'bg-yellow-400'
                }`} />
                <span className="text-xs font-bold text-slate-800 capitalize">
                  {activeMood === 'swiss' && 'Minimal Swiss'}
                  {activeMood === 'editorial' && 'Cozy Editorial'}
                  {activeMood === 'tech' && 'Futuristic Tech'}
                  {activeMood === 'brutalist' && 'Neo-Brutalist'}
                </span>
              </div>
            </div>
          </div>

          {/* Render Active Diagnostics workspace Tab with high-fidelity transition animation */}
          <div className="min-h-[500px]">
            <AnimatePresence mode="wait">
              <motion.div
                key={activeTab}
                initial={{ opacity: 0, y: 8 }}
                animate={{ opacity: 1, y: 0 }}
                exit={{ opacity: 0, y: -8 }}
                transition={{ duration: 0.16 }}
                className="outline-none"
              >
                {activeTab === 'playground' && (
                  <MoodPlayground activeMood={activeMood} setActiveMood={setActiveMood} />
                )}
                {activeTab === 'accessibility' && (
                  <ColorAuditor />
                )}
                {activeTab === 'grid' && (
                  <GridSystem />
                )}
              </motion.div>
            </AnimatePresence>
          </div>
        </div>

        {/* Small Sticky Bottom Studio footer */}
        <footer className="h-12 bg-white border-t border-slate-200 px-8 flex items-center justify-between shrink-0 text-[10px] text-slate-400 font-sans">
          <div className="flex items-center gap-2">
            <span className="font-semibold text-slate-650 font-display">STUDIO CANVAS</span>
            <span className="inline-block w-1 h-3 bg-slate-200 rounded-full" />
            <span>High-Fidelity Engineering Suite</span>
          </div>
          <div className="flex items-center gap-1.5">
            <span>Crafted with</span>
            <Heart className="w-3 h-3 text-red-500 shrink-0 fill-red-500" />
            <span>&copy; {new Date().getFullYear()}</span>
          </div>
        </footer>
      </main>
    </div>
  );
}
